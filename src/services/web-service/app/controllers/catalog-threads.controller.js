const appPath = process.env.WEB_SERVICE_APP_PATH;
const mediaPath = process.env.WEB_SERVICE_MEDIA_PATH;

const catalogThreadsViewApi = (req, res) => {
    res.json({
        response: 'TO-DO',
    });
};


module.exports = {
    catalogThreadsViewApi,
};